Original prompt: Build the extensible foundation for a cross-platform online multiplayer UNO No Mercy-style web game, based on Mattel instruction sheet HVW18, with sign-in, lobbies, complete game logic and durable data; defer rich graphics, sound, video, and chat; publish the site and create a GitHub repository for future contributors.

## Work log

- Fixed the V1.6.2 public/in-game Action-card guide layout regression reported from the wide rules page: the two-column CSS grid no longer stretches the four-card Wild group to the six-card Color group's height, removing the large empty bordered block while preserving the existing responsive stack and native disclosure controls. Added a source regression contract for independent group sizing. Focused and full unit suites, TypeScript, scoped lint, production build, diff-check, and the client performance budget are green. The Product Design browser could not reach the local preview because its local-URL policy rejected the connection, so the code and source contracts are verified but the required fresh rendered design comparison remains explicitly blocked.
- Passed the final V1.6.2 lobby-presence D1/browser acceptance at `/tmp/open-shed-v162-lobby-presence-acceptance/msru6zfe-11c25138`: default-off privacy, exact DTO boundaries, freshness/expiry, sole-host browsing, blank and maximum-length aliases, durable invitation replay/body binding, quotas, bilateral blocks, atomic seat/event/chat floors, concurrent last-seat handling, lost responses, all entry-path presence clearing, signed-out invite recovery, focus restoration, and 320px overflow checks are clean. The required gameplay client and five final UI screenshots were visually inspected; only the deliberately induced lost-response reset was observed. The isolated server is stopped, its Miniflare state is archived with the run, the temporary feature environment is removed, and the original local `.wrangler/state` is restored.
- Completed the V1.6.2 invite-reliability design and implementation boundary: production evidence shows the reported friend link preserved `TPM72W` correctly and failed in the upstream Sites/OpenAI authorization step before the game Worker, so invite-aware mobile recovery guidance is added without mislabeling it as a table error. A new exact-flagged, opt-in player lobby uses separate blank public aliases, 45-second coarse presence, 5-minute invitations, verified sole-host browsing, body-bound durable retries, sender/recipient/pair quotas, bilateral durable blocking, and atomic accept+seat claiming with a one-way public-safe communication downgrade. All identifiers, codes, timestamps, account names, prior events, and pre-join chat remain outside the directory/new-member boundary.
- Started the invitation-reliability and opt-in lobby-presence pass after a mobile guest received OAuth `access_denied` from Sign in with ChatGPT for invite `TPM72W`. The production Site is public and the failure did not reach the Worker, so the join-code/return-path code is not yet implicated; current evidence points to the external authorization step (the user declining, an organization policy, mobile-client state, or the concurrent ChatGPT Free-user incident). The implementation plan is to preserve private invite intent, add friendly recovery/alternative access as warranted by the final auth audit, and add a default-private `Looking for a game` lobby directory with an explicit public alias, short-lived coarse presence, bilateral block filtering, rate-limited one-time invitations, and a one-way `public_safe` communication downgrade when strangers match.
- Completed the V1.6.1 Rules Clarity release from the approved parity audit: one deeply immutable rules registry now powers the public and in-game guides; both surfaces cover the full round flow, every core Action card, the exact 168-card manifest, deterministic online rulings, and the disabled optional points-to-1,000 mode. Results now say Round wins, package/release metadata report 1.6.1, and V1.7 is reserved for optional scoring. The long-form modal adds an early close, keyboard-focus section links, trigger restoration, inert/background scroll containment, semantic inventory, 44px controls, and mobile reflow. Stability now includes privacy-safe route/global recovery screens, bounded runtime contracts, and CI client-bundle budgets while keeping LiveKit lazy. Final gates pass with 276/276 unit tests, TypeScript, full lint, diff checks, zero production dependency vulnerabilities, a clean production build, the required gameplay client, and fresh 1280px/320px/200%-equivalent browser acceptance at `/tmp/open-shed-v161-rules-final-11`; the final bundle is 566.9 KB initial JavaScript, 190.2 KB largest eager chunk, 526.9 KB lazy LiveKit, 1,097.0 KB total client JavaScript, and 86.6 KB CSS.
- Completed the integrated V1.6.0 release gate. The server-verified host-continuity command admits exactly one eligible claimant after the 120-second stale-host grace, rejects a claimant after a host heartbeat without changing the table revision, and replays durably. The immutable round ledger records a deliberately dropped completion response exactly once, preserves scores into round two, bounds viewer reads to the projected revision, and derives trustworthy legacy completion metadata from `game_won` events. The final D1/browser harness passed with clean 1280px and 320px results, no horizontal overflow, 44px+ actions, no browser or cleanup errors, and artifacts at `/tmp/open-shed-v16-continuity-acceptance/mspuwbje-9e20b73a`; the final required game client is clean at `/tmp/open-shed-v16-final-client-fixed.eOpOrx`.
- Completed the V1.6 Game Night Continuity UI binding: eligible players receive a server-projected `Keep table going` recovery action; completed games show the round number, deterministic series ranking, and up to five recent winners; rematches clearly preserve the table and score. Equal-revision polling now refreshes server-time/presence capabilities while rejecting older snapshots. Canonical release identity, package metadata, diagnostics, and About/What’s New report V1.6.0; the next preview is limited to a V1.6.1 stability/accessibility/performance pass. Focused lint, 17 UI/version tests, TypeScript, the required game client, and live 1280/320 browser checks pass with no horizontal overflow or unexpected errors; artifacts are under `/tmp/open-shed-v16-ui-qa-20260812-0950` and `/tmp/open-shed-v16-required-client-20260812-0945`.
- Started the approved release sequence: V1.5.1 will establish one canonical, player-visible app version and distinguish it from rules/profile and per-table revision labels; V1.6.0 “Game Night Continuity” will add server-verified host recovery after the existing grace period plus an immutable round ledger, persistent series scores, recent winners, and rematch continuity. Work is split into bounded release-identity, server/domain, and responsive UI slices before integrated migration, concurrency, browser, and deployment gates.
- Completed the bounded V1.5.1 release-identity foundation: one canonical application version now matches package and lock metadata, document metadata exposes the release separately from per-table revisions, and privacy-safe issue diagnostics include the app version plus an optional sanitized public build slug. Release drift, build-ID filtering, and diagnostic allowlisting passed 15 focused tests, full TypeScript, scoped lint, diff checks, and the required game client (`mode: lobby-browser`, release `1.5.1`, no console errors).
- Completed the invite-only rich-communication slice on `codex/v15-private-live-comms`. Private, never-public tables now support 160-grapheme normalized plain-text chat with durable retries, contact-detail filtering, existing mute/block/report controls, 24-hour messages, and exact-message post-exit reports; public or ever-public tables remain curated-only through a one-way server-owned room scope. The opt-in LiveKit boundary joins listen-only, requests the microphone only after an explicit action, grants short-lived microphone-only tokens, revalidates full eligibility after minting, and durably revokes participants/rooms across block, leave, publication, closure, and expiry. The final D1/browser run at `/tmp/open-shed-v15-chat-acceptance/msp6ph0z-49fbbd78` passed every private-text, safety, real-card-action, post-exit-report, and 320px scenario; the no-credential voice endpoint remained fail-closed with zero microphone access. The required game client, 232 unit tests, SQLite cleanup smoke, TypeScript, full lint, migration regeneration, production build, diff check, production audit, and independent P0/P1 audit are green. Live voice remains disabled until production LiveKit credentials and scheduled cleanup are configured; stored voice notes remain deferred because the hosted project has no R2 binding or guaranteed object-deletion scheduler.
- Completed the opt-in audio/VFX pass on `codex/v15-audio-vfx`: original procedural cues distinguish number, action, draw, Wild, turn, UNO, Mercy, and win moments; optional device-synthesized callouts announce only the public card color/value; settings persist locally while every browser session still requires an explicit unlock; hidden tabs may emit only the self-turn chime; hydration, reconnect catch-up, duplicate revisions, and table changes stay silent; and the discard/turn emphasis has a reduced-motion alternative. Runtime mappings reject malformed cards, prototype keys, fractional penalties, names, event prose, and private identifiers. The required client, desktop settings, reload-ready state, 320px controls, reduced motion, and a live two-player Wild Color Roulette transition all pass: both clients received the bounded callout, the next player received the turn cue, both discard pulses rendered, and no unexpected browser errors occurred. Final gates pass with 185/185 unit tests, TypeScript, full lint, a clean production build, zero production dependency vulnerabilities, and diff checks.
- Resolved the production report from table `X7AV5W` as a turn-clarity defect rather than an engine deadlock. The retained trail and a deterministic two-player regression prove Color Roulette correctly asks the next player to choose a color, then returns an actionable turn to the actor; the UI now explains that before play, identifies the Roulette chooser while waiting, and labels the returned action `Your turn — draw until playable` with the active color.
- Added a global privacy-safe `Report issue` flow for signed-in players in the lobby browser, table lobby, active play, and results. It creates an editable public GitHub draft only after an explicit click, offers a local copy fallback, shares only constructed allowlisted diagnostics, blocks names/codes/IDs/links/email/control text, routes security reports to enabled private vulnerability reporting, and never sends raw game state, hands, events, chat, or account data. Independent desktop, active-game, result, 320px, 200%-equivalent, focus, copy, URL, and required-client QA passed with 176/176 unit tests, TypeScript, full lint, production build, zero production dependency vulnerabilities, and no browser errors.
- Final Epic 3 release gates are green: 164/164 unit and source-contract tests, full TypeScript and lint, a production build containing all five communication routes, migration regeneration with no drift, disabled-switch regression on every route, and zero production dependency vulnerabilities. Communication and discovery still default off; free text remains off even when curated communication is enabled.
- Passed the tightened Epic 3 live D1/browser acceptance at `/tmp/open-shed-v15-chat-acceptance/msozshux-1a167ae2`: all 13 scenario groups covered solo-lobby visibility, two-member ordered phrases/reactions, durable replay, reports, refresh-persistent mute/block, retained report evidence, leave denial, chat/game isolation, and a real legal card action. Desktop and 320px screenshots show Chat and Activity in both lobby and active play; mobile has no horizontal overflow or turn obstruction, and the final Unmute/Unblock controls measure at least 76×44. The required game client and semantic text capture are also clean.
- Completed the Epic 3 server boundary: a generated D1 migration and separate feature-gated communication service now provide member-only curated messages, atomic two-second cooldown and durable retry handling, 24-hour message expiry, table-scoped mute, persistent block, privacy-safe report authorization, and independent 90-day allowlisted report evidence without changing gameplay state, revisions, events, or presence. The full unit suite, TypeScript, lint, migration drift, and focused privacy contracts were green before the successful live acceptance run.
- Started V1.5 Epic 3 on `codex/v15-epic3-safe-chat` after confirming that the hosted Epic 1 build contains no chat service. The implementation target is a separate feature-gated, table-member-only `Chat | Activity` service with allowlisted quick phrases/reactions, mute/block/report controls, 24-hour message retention, accessible mobile UI, and no arbitrary free text.
- Completed V1.5 Epic 2 privacy-first open-table discovery on `codex/v15-epic2-open-tables`. Hosts can explicitly publish and unpublish with a separate room alias and pace; signed-out visitors receive capped, cache-safe anonymous availability; signed-in players can select a table or use bounded three-candidate Quick Join; private code joins unlist a public table; and all public joins are guarded, durable, atomic, capacity-safe, block-aware, and event-floor isolated from pre-public names and events.
- Verified Epic 2 with 135 passing unit tests, full TypeScript and lint, a clean production build exposing all five discovery routes, zero production dependency vulnerabilities, independent P0/P1 privacy and concurrency audits, and a real local D1/browser acceptance journey rerun after the final hardening. The run covered profile-free public reads, exact DTO allowlists, blank strict aliases, sign-in intent, public publish/join, anonymous six-card versus signed-in pagination, Quick Join, atomic manual-code profiles and unlisting, private-locator closure privacy, full/closed/unavailable races, lost-response replay, 320px touch/focus behavior, clean screenshots, and cleanup with no unexpected console errors. Public discovery remains disabled in production until a separate staged-rollout decision.
- Deployed V1.5 Epic 1 as Sites version 5 and verified the hosted D1 migration with a signed-in production create → final leave → room removal → closed-link flow. The temporary room disappeared immediately and its stale link returned `This room is closed.`; discovery, communication, and free text remain disabled.
- Started V1.5 Epic 1 from `docs/v1.5-open-tables-safe-talk.md`: closed-room tombstones, explicit-final-leave closure, abandoned-lobby cleanup, fail-closed feature switches, profile-block storage, schema migration, and lifecycle regression coverage. Public discovery and communication remain disabled until later epics.
- Implemented the Epic 1 vertical slice: completed members can explicitly leave with host transfer and card conservation; final departure atomically records `room_emptied`/`room_closed`, hides the room, removes presence, and starts a 24-hour tombstone; stale waiting rooms close after the 45-second disconnect boundary plus five-minute grace; closed reads and joins return `ROOM_CLOSED`; discovery/communication/free-text switches default off; and profile-block/discovery-consent storage is migrated.
- Verified Epic 1 with 76 passing unit tests, TypeScript, lint, production build, migration regeneration, direct local D1 API flows for empty and abandoned closure, lost-response leave replay, completed-table departures, and required gameplay-client screenshots/text for results → `Leave table and go back` → lobby plus the closed-link error state. Lifecycle sweeps now revalidate heartbeats, avoid attended-room starvation, isolate corrupt candidates, preserve original `left_at`, and map closure races to `ROOM_CLOSED`.
- Started V1.1 “Game Night Loop” implementation: one-tap invitation/joining, player-focused readiness, contextual turn guidance and in-game rules, visible connection recovery, completed-game results/rematch, mobile hierarchy, and end-to-end release gates. Rich chat/media, matchmaking, rankings, and alternate modes remain deferred.
- Pinned the V1.1 product and acceptance contract in `docs/v1.1-game-night-loop.md`, taught the server-rendered sign-in path to preserve valid `?join=CODE` invitations, and captured a clean required-game-client baseline (`mode: lobby-browser`) before the larger changes.
- Initialized the Sites starter and started the local preview.
- Selected a server-authoritative, persistence-backed multiplayer baseline; implementation and rule verification are in progress.
- Added the versioned 168-card manifest, Web Crypto production shuffle with deterministic test fixtures, isolated rules reducer, viewer-safe projection, UNO reaction model, stacking, Mercy, 0/7, every action card, draw recycling, and card-conservation invariants.
- Expanded the automated suite to 35 passing engine and server-guard scenarios. It caught and drove fixes for UNO windows reopening, forced-card privacy, final-effect precedence, roulette exhaustion, departure transients, body limits, and cross-site mutation rejection.
- Added D1-backed profiles, games, memberships, and append-only public event records; generated and inspected the first migration.
- Added authenticated JSON APIs for session, private lobby listing/creation/joining, private game views, bounded event feeds, and versioned durable-idempotent commands. A local two-player create/join/ready/start smoke test passed with opponent hands and draw order redacted.
- Production build passes with all API routes detected.
- Added the responsive lobby/table/hand client, durable game URLs, adaptive reconnect polling, public remote-event updates, success-only leave flow, deterministic browser hooks, and keyboard-accessible card-choice dialogs.
- Hardened the baseline with cryptographic production shuffles, private forced-card projection, atomic persistence design, durable receipts, quotas, lazy retention cleanup, and dependency updates with zero production audit findings.
- Re-ran a real local D1 two-player API flow: create/join/ready/start, private projections, event cursors, durable create/start/leave replays, stale revision rejection, solo and mid-game leave, departed-member access denial, private lobby listing, and streamed oversize rejection all passed.
- Re-ran desktop and 320px browser regressions plus the required gameplay client; deep links, live remote events, ID-based turns, dialog focus/Escape, mobile controls, canvas state, and console checks passed.
- Selected the layered Option 1 card direction and began the implementation: added an exhaustive card-presentation model, a reusable semantic DOM card face, Phosphor action icons, a generated charcoal material texture, and shared hand/table card rendering without changing rules logic.
- Finished Option 1 card QA: corrected the initial dense overlap, verified live remote turns, playable/focus cues, wild-choice focus restoration, a 320px horizontally scrollable hand, and a clean fresh-tab console. The design comparison passes with no P0/P1/P2 findings; 47 unit tests, typecheck, lint, production build, and the production dependency audit pass.
- Replaced the unsupported local-development `window.prompt()` identity switcher with an in-page modal: labelled form, trimmed 28-character names, inline empty-name errors, one-shot identity writes, initial input focus, trapped Tab navigation, Escape/Cancel focus restoration, and inert background content.
- Verified the test-player switch end to end in isolated and live browser sessions: no native JS dialog, one DOM dialog, whitespace validation, forward/backward focus trap, Escape/Cancel restoration, successful identity save + reload, usable 320×900 layout, matching `render_game_to_text`, and zero page/console errors. All 47 unit tests, TypeScript, scoped lint, diff checks, and the production build pass (build requires the bundled modern Node runtime; the shell default Node 20 lacks `fs.promises.glob`).
- Rebuilt the signed-out experience as a player-facing front door: a restrained Option 1 card fan, direct sign-in and rules paths, share-link-safe authentication return targets, quick facts, complete turn flow, power rules, illustrated colored/wild action references, pinned online edge-case decisions, and an official-source/unofficial-project disclaimer. The responsive implementation uses semantic sections, native keyboard-operable disclosures, and the real in-game `CardFace` system rather than decorative stand-ins; TypeScript and scoped lint pass.
- Finished signed-out landing QA in a true production-mode unauthenticated session. At 1903×914 and 320×900, the hero, rules anchors, 2×2 mobile card stage, action-card guide, final CTA, and footer have no horizontal overflow; shared-room sign-in preserves the deep link; all 10 action types and pinned UNO/Mercy/Roulette/final-card decisions were independently copy-checked. The required game client reports `mode: signed-out` with no console errors. All 47 unit tests, full lint/typecheck, the final production build, and the production dependency audit pass.
- Finished the V1.1 Game Night Loop locally. Invite URLs now open a one-tap join confirmation and survive sign-in; the lobby explains exactly who still needs to ready up; players get a persistent rules/action guide and contextual turn coach; commands survive a lost response with the original durable idempotency key; connection state remains visible on desktop and mobile; and completed tables have results, same-room host rematch, back-to-games, and new-table paths.
- Added server-backed presence with live/reconnecting/disconnected/removable thresholds, authenticated heartbeats, host-only two-minute inactive-player removal, atomic staleness revalidation, cleanup on leave/expiry, and viewer-safe presence projections. Complete tables keep low-rate polling and heartbeats so every player observes a rematch without becoming falsely removable.
- Closed two V1.1 concurrency edge cases: receipt recovery now revalidates the request hash before replaying a command, and a target that leaves during inactive-player removal returns a safe version conflict instead of a false corruption error.
- Final V1.1 release gates pass: 58 unit tests, TypeScript, full lint, diff checks, zero production dependency vulnerabilities, a clean production build, the required bundled gameplay client with matching `render_game_to_text`, and a two-context browser acceptance flow covering invitation, join, deliberately lost ready response, same-command retry, start, 320px layout, offline recovery, and rules-dialog Escape behavior.

## TODO

- Stage Epic 2 through an internal production cohort only after explicitly enabling the discovery switch, then repeat hosted privacy, join, race, mobile, and rollback smoke tests before widening access.
- Keep private free text off in production until the documented child-access, moderation, urgent-harm, privacy, and rollback gates have named owners and passed an internal cohort.
- Configure and approve a managed LiveKit project, hosted URL/key/secret, provider-side room policy, and voice-incident response process before enabling the independent live-voice switch.
- Add a private R2 binding plus lifecycle/scheduled hard deletion, codec/container validation, and evidence handling before implementing stored voice notes.
- Add Durable Object/WebSocket transport when the hosting contract supports it; keep the current command and projection boundary.
- Add deeper automated D1 concurrency and migration integration coverage before enabling either public V1.5 capability.

## Pinned baseline rule decisions

- Exact per-card quantities are independently corroborated because Mattel's sheet states 168 cards but does not print the manifest.
- A final card wins before external 0/7/draw effects; Discard All's same-color removals are part of that final play.
- Mercy fires immediately on card 25 for ordinary draws and penalties; roulette adds its revealed batch before checking Mercy.
- Wild +4 reverse, +6, and +10 use the actor's chosen continuing color; roulette uses the target's choice.
- A missing roulette color drains all currently recyclable cards and ends safely rather than looping.
